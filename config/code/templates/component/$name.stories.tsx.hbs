import type { Story, StoryDefault } from "@ladle/react"
import * as {{ name }} from "./{{ name }}"

export default { title: "__group__{{ name }}" } satisfies StoryDefault

export const Basic: Story = () => <{{ name }}.Root />
