@layer sui.components.{{ kebab name }}.static {
  @scope (.Sui{{ name }}) to (:not([data-scope~=Sui{{ name }}])) {
    :scope {
      /*
       * Root
       * 
       */

      &:not(:is(:disabled, [disabled], [data-disabled])) {}
    }
  }
}

@layer sui.components.{{ kebab name }}.dynamic;

@layer sui.disabled {
  @scope (.Sui{{ name }}) to (:not([data-scope~=Sui{{ name }}])) {
    :scope:is(:disabled, [disabled], [data-disabled]) {}
  }
}
