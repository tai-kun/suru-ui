export function __basename_outpath__() {}

if (cfgTest && cfgTest.url === import.meta.url) {
  const { describe, test } = cfgTest;

  describe("@suru-ui/{{name}}/__dirname_outpath__/__basename_outpath__", () => {
    test.todo("テスト");
  });
}
