export function {{basename outpath}}() {}

if (cfgTest && cfgTest.url === import.meta.url) {
  const { describe, test } = cfgTest;

  describe("@suru-ui/{{normalize outpath}}", () => {
    test.todo("テスト");
  });
}
