export function {{normalize outpath}}() {}

if (cfgTest && cfgTest.url === import.meta.url) {
  const { describe, test } = cfgTest;

  describe("@suru-ui/vhash/{{normalize outpath}}", () => {
    test.todo("テスト");
  });
}
